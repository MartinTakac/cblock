// Vertex shader program is required by the pipeline, but nothing in this 
// is actually used since the vertex positions are being passed as shader 
// uniforms directly to the geometry stage.

void main(void)
{
}
